DB_NAME=task_management
DB_USERNAME=task_user
DB_PASSWORD=task_password
DB_PORT=5432

# Base64-encoded HS256 signing key (>= 32 raw bytes). Generate your own with:
#   python3 -c "import base64, os; print(base64.b64encode(os.urandom(32)).decode())"
# The value baked into application.yml as a fallback is for local/dev only —
# the aws profile has no default and refuses to start without this set.
JWT_SECRET=7rAVNtHkuy3lZ9fhDFHXgACpPLgzkmkKC8DGyOhAWTI=
JWT_EXPIRATION_MINUTES=60
